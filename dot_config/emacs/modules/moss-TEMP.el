;;; moss-TEMP.el --- Summary

;;; Commentary : ---------------------------------------------------------------

;;; Code : ---------------------------------------------------------------------

(message "[ Moss ] Loading module, TEMP  ... ")

(use-package TEMP
  ;; :bind
  ;; (
  ;; ("C-c a" . org-agenda) ;; example
  ;; )
  ;; :config
  ;; :init
  ;; :?
  )

(provide 'moss-TEMP)
;;; moss-TEMP.el ends here -----------------------------------------------------
