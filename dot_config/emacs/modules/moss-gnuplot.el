;;; moss-gnuplot.el --- Summary

;;; Commentary : ---------------------------------------------------------------

;;; Code : ---------------------------------------------------------------------

(message "[ Moss ] Loading module, gnuplot  ... ")

(use-package gnuplot
  ;; :bind
  ;; (
  ;; ("C-c a" . org-agenda) ;; example
  ;; )
  ;; :config
  ;; :init
  ;; :?
  )

(provide 'moss-gnuplot)
;;; moss-gnuplot.el ends here -----------------------------------------------------
