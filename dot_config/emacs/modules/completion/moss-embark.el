;;; moss-embark.el --- Summary

;;; Commentary : ---------------------------------------------------------------

;;; Code : ---------------------------------------------------------------------

(message "[ Moss ] Loading module, embark  ... ")

(use-package embark
  ;; :bind
  ;; (
  ;; ("C-c a" . org-agenda) ;; example
  ;; )
  ;; :config
  ;; :init
  ;; :?
  )

(provide 'moss-embark)
;;; moss-embark.el ends here -----------------------------------------------------
